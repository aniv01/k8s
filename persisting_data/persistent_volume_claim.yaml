apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-names
spec:
  storageClassName: manual
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
